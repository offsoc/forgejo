{{template "base/head" .}}
<div role="main" aria-label="{{.Title}}" class="page-content explore gists">
	{{template "explore/navbar" .}}
	<div class="ui container">
		{{template "explore/search" .}}
		{{template "explore/gist_list" .}}
		{{template "base/paginate" .}}
	</div>
</div>
{{template "base/footer" .}}
