
<div class="header">
	{{ctx.Locale.Tr "sponsor.header_text" .FundingName}}
</div>
<div class="content">
	<ul>
		{{range .Funding}}
			<li>
				<div class="ui">
					{{if .Icon}}
						<img class="ui tw-h-full tw-align-middle" src="{{.Icon}}" width="16" height="16">
					{{else}}
						{{svg "octicon-heart"}}
					{{end}}

					<a href="{{.URL}}">{{.Text}}</a>
				</div>
			</li>
		{{end}}
	</ul>
</div>
